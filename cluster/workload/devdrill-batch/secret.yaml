apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: devdrill-batch-externalsecret
  namespace: devdrill-prod
spec:
  refreshInterval: "30s"
  secretStoreRef:
    name: doppler-store
    kind: SecretStore
  target:
    name: devdrill-batch-secret
    creationPolicy: Owner
  dataFrom:
    - find:
        name:
          regexp: DB.*
